## 1. GuildScreen基盤

- [x] 1.1 GuildScreen (.tscn + .gd) を作成。Guild・DataLoaderインスタンスを保持し、子シーン差し替えメソッド `_switch_view()` を実装する
- [x] 1.2 GuildScreenのテスト: _switch_view()による子ノード差し替えが正しく動作することを検証する

## 2. GuildMenu（ギルドメニュー画面）

- [x] 2.1 GuildMenu (.tscn + .gd) を作成。4つのメニュー項目を表示し、選択時にsignalを発行する
- [x] 2.2 GuildMenuのテスト: 各メニュー項目の選択で正しいsignalが発行されることを検証する
- [x] 2.3 GuildScreenにGuildMenuを統合し、メニュー選択から各画面への遷移を接続する

## 3. CharacterCreation（キャラクター作成ウィザード）

- [x] 3.1 CharacterCreation (.tscn + .gd) の基本構造を作成。5ステップの切り替えロジックと「戻る」「次へ」「やめる」ボタンを実装する
- [x] 3.2 Step 1（名前入力）を実装。テキスト入力フィールドと空名バリデーションを追加する
- [x] 3.3 Step 2（種族選択）を実装。DataLoaderから種族一覧を取得し、ステータス合計値付きのリストを表示する
- [x] 3.4 Step 3（ボーナスポイント配分）を実装。BonusPointGeneratorによるポイント生成、+/-ボタン、残りポイント表示、振り直しボタンを追加する
- [x] 3.5 Step 4（職業選択）を実装。全職業リスト表示、can_qualify()による選択可否判定、不可職のグレーアウトを追加する
- [x] 3.6 Step 5（確認画面）を実装。全情報表示とCharacter.create() + Guild.register()による作成処理を追加する
- [x] 3.7 ステップ間の「戻る」挙動を実装。Step 3→Step 2でボーナス配分リセット、Step 3再入時に新規ポイント生成する
- [x] 3.8 CharacterCreationのテスト: 各ステップの表示内容、バリデーション、ステップ遷移、戻り時のリセットを検証する

## 4. PartyFormation（パーティ編成画面）

- [x] 4.1 PartyFormation (.tscn + .gd) を作成。パーティグリッド（前列3×後列3）と待機キャラクターリストを表示する
- [x] 4.2 スロット選択→キャラ追加フローを実装。空スロット選択時に待機リストから選択してGuild.assign_to_party()を呼び出す
- [x] 4.3 スロット選択→キャラ除外フローを実装。占有スロット選択時にGuild.remove_from_party()を呼び出す
- [x] 4.4 パーティ名表示・編集機能を実装。画面上部のパーティ名を選択で編集可能にする
- [x] 4.5 PartyFormationのテスト: キャラ追加・除外・パーティ名変更の各操作を検証する

## 5. CharacterList（キャラクター一覧画面）

- [x] 5.1 CharacterList (.tscn + .gd) を作成。全キャラクターを名前・LV・種族・職業・所属状態で一覧表示する
- [x] 5.2 キャラクター詳細表示を実装。選択キャラの全ステータスを表示するサブビューを追加する
- [x] 5.3 キャラクター削除機能を実装。パーティ未所属キャラのみ削除可能、「はい/いいえ」確認ダイアログ付きでGuild.remove()を呼び出す
- [x] 5.4 CharacterListのテスト: 一覧表示、詳細表示、削除（成功・キャンセル・パーティ所属時の拒否）を検証する

## 6. 統合・動作確認

- [x] 6.1 全画面間の遷移を結合テスト。メニュー→各画面→メニューの往復が正しく動作することを検証する
- [x] 6.2 main.tscnまたはテスト用エントリーポイントからGuildScreenを起動できるようにする
